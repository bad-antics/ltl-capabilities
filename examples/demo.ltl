// examples/demo.ltl — runnable demo for ltl_capabilities

import std::io
use ltl_capabilities as lib

fn main() {
    io::println("ltl_capabilities demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}